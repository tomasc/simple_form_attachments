development:
  clients:
    default:
      database: dummy_development
      hosts:
        - <%= ENV['MONGODB_URL'] || "localhost:27017" %>

test:
  clients:
    default:
      database: dummy_test
      hosts:
        - <%= ENV['MONGODB_URL'] || "localhost:27017" %>
